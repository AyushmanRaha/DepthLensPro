const path = require("path");
function createSplashWindow({ BrowserWindow, dirname }) { const splashWindow = new BrowserWindow({ width: 480, height: 300, frame: false, transparent: true, resizable: false, alwaysOnTop: true, webPreferences: { nodeIntegration: false, contextIsolation: true } }); splashWindow.loadFile(path.join(dirname, "src", "splash.html")); splashWindow.center(); return splashWindow; }
function createMainWindow({ BrowserWindow, shell, log, dirname, frontendPath, isDev, isExternalUrl, getSplashWindow = () => null }) { const mainWindow = new BrowserWindow({ width: 1440, height: 900, minWidth: 900, minHeight: 600, show: false, titleBarStyle: process.platform === "darwin" ? "hidden" : "default", trafficLightPosition: process.platform === "darwin" ? { x: 16, y: 18 } : undefined, vibrancy: "under-window", visualEffectState: "active", backgroundColor: "#070d17", webPreferences: { nodeIntegration: false, contextIsolation: true, sandbox: true, preload: path.join(dirname, "preload.js"), webSecurity: true } }); mainWindow.loadFile(frontendPath); mainWindow.once("ready-to-show", () => { const splashWindow = getSplashWindow(); if (splashWindow && !splashWindow.isDestroyed()) splashWindow.destroy(); mainWindow.show(); mainWindow.focus(); log.info("Main window shown"); }); mainWindow.webContents.setWindowOpenHandler(({ url }) => { if (isExternalUrl(url)) shell.openExternal(url); else log.warn(`Blocked new-window request to: ${url}`); return { action: "deny" }; }); if (isDev && process.env.DEPTHLENS_OPEN_DEVTOOLS === "1") mainWindow.webContents.openDevTools({ mode: "detach" }); return mainWindow; }
module.exports = { createSplashWindow, createMainWindow };
